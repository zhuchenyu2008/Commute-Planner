services:
  commute-planner:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      DATABASE_URL: "file:/app/data/commute.db"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
